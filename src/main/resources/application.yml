spring:
  profiles:
    active: prod
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql:///girlDB?useSSL=false
    username: root
    password: 5094022aA
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
